# Railway monorepo configuration

# Frontend service (default, uses root Dockerfile)
[service]
name = "frontend"

# Backend service override
[[services]]
name = "backend"
dockerfile = "backend/Dockerfile.backend"
context = "backend"
